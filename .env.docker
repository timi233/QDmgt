# ============================================
# QDmgt Docker环境配置文件
# ============================================
# 说明：复制此文件为 .env.docker 并修改配置
# 命令：cp .env.docker.example .env.docker

# ============================================
# PostgreSQL数据库配置
# ============================================
# 数据库用户名
POSTGRES_USER=qdmgt_user

# 数据库密码（必须修改为强密码！）
# 建议：至少16字符，包含大小写字母、数字、特殊字符
POSTGRES_PASSWORD=Gn3WfqYjcr6Bsum123

# 数据库名称
POSTGRES_DB=qdmgt_db

# ============================================
# 后端应用配置
# ============================================
# 数据库连接URL（自动从上述配置组装，通常无需修改）
DATABASE_URL=postgresql://qdmgt_user:Gn3WfqYjcr6Bsum123@postgres:5432/qdmgt_db

# CORS允许的源（根据实际部署地址修改）
# 支持多个地址，用逗号分隔
SECURITY_ALLOWED_ORIGINS=http://192.168.101.9:3002,http://localhost:3002,http://127.0.0.1:3002

# JWT密钥（用于签发访问令牌，必须修改！）
# 生成方法：openssl rand -base64 48
JWT_SECRET_KEY=Yev8l2Spz5wwQpi1oTdZn4q0WQ8xpo+ybFqPj9iJwGvK4WzRRg6PFOA/8YgYWYqm
# 通用加密密钥（用于其他加密操作，必须修改！）
# 生成方法：openssl rand -base64 48
SECRET_KEY=J69ndcv/3a930wN4g45X9YpxkiaNiv5a8Uw13A/3MlLEmq7UdE8H7YFl5cwLjO/T

# 运行环境（production/staging/development）
ENVIRONMENT=production

# 调试模式（生产环境建议设为false）
DEBUG=false

# 应用名称
APP_NAME=Channel Management System

# 应用版本
VERSION=0.1.0

# JWT访问令牌过期时间（分钟）
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# JWT刷新令牌过期时间（天）
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================
# 前端配置
# ============================================
# 后端API地址（构建时注入，根据实际部署地址修改）
REACT_APP_API_BASE_URL=http://192.168.101.9:8001/api/v1

# ============================================
# 服务端口配置（可选）
# ============================================
# 后端API端口（默认8001）
BACKEND_PORT=8001

# 前端Web端口（默认3002）
FRONTEND_PORT=3002

# ============================================
# 部署说明
# ============================================
# 1. 修改POSTGRES_PASSWORD为强密码
# 2. 使用openssl生成JWT_SECRET_KEY和SECRET_KEY
# 3. 修改SECURITY_ALLOWED_ORIGINS为实际访问地址
# 4. 修改REACT_APP_API_BASE_URL为实际后端地址
# 5. 保存为 .env.docker 文件
# 6. 确保.env.docker已加入.gitignore（不要提交到版本控制）
#
# 安全提示：
# - 生产环境必须使用强密码和随机密钥
# - 不要在代码仓库中提交.env.docker文件
# - 定期更换密钥（建议每季度）
# - 限制CORS允许的源地址范围
